ARG REDIS_VERSION

FROM redis:${REDIS_VERSION}

MAINTAINER david

ENTRYPOINT echo "yes"|redis-cli --cluster create ${REDIS_DOMAIN1} ${REDIS_DOMAIN2} ${REDIS_DOMAIN3} ${REDIS_SLAVE_DOMAIN1} ${REDIS_SLAVE_DOMAIN2} ${REDIS_SLAVE_DOMAIN3} --cluster-replicas 1
